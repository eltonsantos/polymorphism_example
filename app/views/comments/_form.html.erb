<%= simple_form_for [commentable, Comment.new] do |f| %>

  <div class="form-inputs">
    <%= f.input :body, placehoulder: "Add comment" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
